const [a, b, c, d] = [7, 5, 1, 25]

const soma = a + b + c + d
const subtracao = d - a
const mult = a * b
const divisao = d / a
const modulo = a % 2

console.log(`
Soma: ${soma}
Subtração: ${subtracao}
Multiplicação: ${mult}
Divisão: ${divisao}
Modulo: ${modulo}
`)